{
  "name": "regimen-works",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "postinstall": "npm run build --workspace packages/shared && npm run prisma:generate --workspace apps/api",
    "hooks:install": "git config core.hooksPath .githooks",
    "security:scan": "gitleaks git --no-banner --redact --verbose -c .gitleaks.toml .",
    "security:audit": "npm audit --omit=dev --audit-level=moderate",
    "dev:api": "npm run start:dev --workspace apps/api",
    "dev:web": "npm run dev --workspace apps/web",
    "build": "npm run build --workspace packages/shared && npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces",
    "lint:fix": "npm run lint:fix --workspaces",
    "test": "npm run build --workspace packages/shared && npm run test --workspaces --if-present",
    "test:cov": "npm run build --workspace packages/shared && npm run test:cov --workspaces --if-present",
    "typecheck": "npm run build --workspace packages/shared && npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=22.10.0"
  },
  "overrides": {
    "deepmerge-ts": "^8.0.2",
    "multer": "^2.3.0",
    "mysql2": "^3.24.4"
  }
}
